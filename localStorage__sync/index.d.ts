export declare function localStorage__sync(key:string, value:string):string
export {
	localStorage__sync as sync_localStorage,
	localStorage__sync as sync__localStorage,
	localStorage__sync as store__localStorage,
}
